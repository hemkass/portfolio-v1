import Modal from "./components/modal";
const MonCV = ({ setCV, setCarousel }) => {
  return (
    <div>
      <Modal setCV={setCV} setCarousel={setCarousel}>
        <div>MonCV</div>
      </Modal>
    </div>
  );
};

export default MonCV;
