import Modal from "./modal";

const CV = ({ setCV }) => {
  return (
    /*    <Modal setCV={setCV}> */
    <div>
      <h5>CECI EST MON CV</h5>
    </div>
  );
  {
    /*     </Modal> */
  }
};

export default CV;
